#import "../fragments/track.graphql"

query GetListenedNow(
    $pageLimit: Int,
    $pageNumber: Int
) {
    GetListenedNow(
        page: $pageNumber,
        limit: $pageLimit
    ) {
        data {
            ...track
        }
    }
}
