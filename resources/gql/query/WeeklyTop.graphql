#import "../fragments/track.graphql"

query TopWeeklyQuery(
    $pageLimit: Int,
    $pageNumber: Int
) {
    TopWeeklyQuery(
        page: $pageNumber,
        limit: $pageLimit
    ) {
        total
        to
        data {
            ...track
        }
    }
}
