#import "./role.graphql"
#import "./location.graphql"

fragment user on User {
    id
    username
    avatar(
        sizes: [size_56x56, size_235x235]
    ) {
        size
        url
    }
    roles {
        ...role
    }
    location {
        ...location
    }
    followersCount
#    description # api bug: cannot use right now
    iWatch
}
