query MyHeaderProfile {
    myProfile {
        id
        avatar(
            sizes: [size_56x56]
        ) {
            size
            url
        }
        roles {
            slug,
            name
        }
    }
}
