mutation AddToCollection(
    $collectionId: Int!
    $trackIds: [Int]!
) {
    addInCollection(
        tracksId: $trackIds,
        collectionId: $collectionId
    ) {
        id
        title
        image
    }
}
