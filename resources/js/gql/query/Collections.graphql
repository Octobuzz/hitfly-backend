#import "../fragments/collection.graphql"

query Collections(
    $pageLimit: Int!,
    $pageNumber: Int!,
    $my: Boolean
) {
    collections(
        limit: $pageLimit,
        page: $pageNumber,
        my: $my
    ){
        total
        to
        data {
            ...collection
        }
    }
}
