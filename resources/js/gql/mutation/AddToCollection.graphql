mutation AddToCollection(
    $collectionId: Int!
    $trackIds: [Int]!
) {
    addInCollection(
        tracksId: $trackIds,
        collectionId: $collectionId
    ) {
        id
        title
#        image(
#            sizes: [size_150x150, size_214x160]
#        ) {
#            size
#            url
#        }
    }
}
