mutation CreateCollection(
    $title: String!
    $trackIds: [Int]!
) {
    createCollection(
        title: $title
        tracksId: $trackIds
    ) {
        id
        title
        image
    }
}
