query LifeHacksCategoryContainer_tag(
    $pageNumber: Int!,
    $pageLimit: Int!
) {
    tag(
        page: $pageNumber,
        limit: $pageLimit
    ) {
        total
        to
        data {
            id
            name
        }
    }
}
