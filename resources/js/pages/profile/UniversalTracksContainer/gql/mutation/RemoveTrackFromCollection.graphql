mutation RemoveTrackFromCollection(
    $trackId: Int
    $collectionId: Int
) {
    removeTrackFromCollection(
        trackId: $trackId
        collectionId: $collectionId
    ) {
        id
    }
}
