query TrackBelongsToCollection(
    $trackId: Int,
    $collectionId: Int
) {
    trackBelongsCollection(
        trackId: $trackId
        collectionId: $collectionId
    ) {
        inCollection
    }
}
