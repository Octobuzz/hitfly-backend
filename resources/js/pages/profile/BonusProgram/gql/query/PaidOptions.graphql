query BonusProgram_products {
    products(limit: 30, page: 1) {
        data {
            id
            name
            description
            price
            alias
            image(
                sizes: size_290x290
            ) {
                size,
                url
            }
        }
    }
}
