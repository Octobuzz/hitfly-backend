mutation UpdatePassword(
    $password: String,
    $confirmPassword: String,
    $oldPassword: String
) {
    updatePassword(
        password: {
            password: $password,
            confirmPassword: $confirmPassword,
            oldPassword: $oldPassword
        },
    ) {
        id
    }
}
