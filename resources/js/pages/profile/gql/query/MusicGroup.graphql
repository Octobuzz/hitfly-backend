query MusicGroup(
    $id: Int!
) {
    musicGroup(id: $id){
        id
        name
        avatarGroup
        careerStartYear
        genres {
            id
            name
        }
        description
        location {
            title
        }
        followersCount
    }
}
