query MusicGroup(
    $id: Int!
) {
    musicGroup(id: $id){
        id
        name
        avatarGroup
        careerStartYear
        genre {
            id
            name
        }
        description
        location {
            title
        }
        followersCount
    }
}
