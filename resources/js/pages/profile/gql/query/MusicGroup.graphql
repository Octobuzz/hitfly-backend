query MusicGroup(
    $id: Int!
) {
    musicGroup(id: $id){
        id
        name
        careerStartYear
        genres {
            id
            name
        }
        description
        location {
            title
        }
        followersCount
    }
}
