#import "gql/fragments/user.graphql"

query WathedUsers {
    watchingUser(
        page: 1,
        limit: 3
    ) {
        data {
            id
            user {
                ...user
            }
        }
    }
}
